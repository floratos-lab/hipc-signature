# notice

This docker is not maintained. The current Dockerfile does not build as is.
At this point, it is apparently easier to deploy without using docker.
This directory is kept in case we need to use docker in the future.